name = "kakao-proxy"
main = "kakao-proxy.js"
compatibility_date = "2024-11-01"

# 허용 도메인 제한(권장). 콤마로 여러 개. 비우면 전체 허용(테스트용).
# 배포 후 GitHub Pages 주소로 바꾸세요.
[vars]
ALLOW_ORIGINS = "https://hwkim3330.github.io,http://localhost:8000"

# KAKAO_REST_KEY 는 코드/이 파일에 넣지 말고 시크릿으로:
#   wrangler secret put KAKAO_REST_KEY
