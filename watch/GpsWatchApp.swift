import SwiftUI

// watchOS 앱 진입점.
// Xcode 에서 새 watchOS App 프로젝트를 만든 뒤 이 파일들로 교체하세요(아래 watch/README.md 참고).
@main
struct GpsWatchApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
